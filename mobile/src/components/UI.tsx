import { PropsWithChildren } from 'react';
import { ActivityIndicator, Pressable, StyleSheet, Text, TextInput, TextInputProps, View } from 'react-native';
import { colors } from '../theme';

export function Field({ label, ...props }: TextInputProps & { label: string }) { return <View style={styles.field}><Text style={styles.label}>{label}</Text><TextInput placeholderTextColor="#94a3b8" {...props} style={[styles.input, props.multiline && styles.multiline, props.style]} /></View>; }
export function Button({ title, onPress, loading, secondary, danger, disabled }: { title: string; onPress: () => void; loading?: boolean; secondary?: boolean; danger?: boolean; disabled?: boolean }) { return <Pressable disabled={loading || disabled} onPress={onPress} style={({ pressed }) => [styles.button, secondary && styles.secondary, danger && styles.danger, (pressed || disabled) && styles.dim]}>{loading ? <ActivityIndicator color={secondary ? colors.primary : '#fff'} /> : <Text style={[styles.buttonText, secondary && styles.secondaryText]}>{title}</Text>}</Pressable>; }
export function Card({ children }: PropsWithChildren) { return <View style={styles.card}>{children}</View>; }
const styles = StyleSheet.create({ field: { gap: 7, marginBottom: 16 }, label: { color: colors.text, fontWeight: '700' }, input: { borderWidth: 1, borderColor: colors.border, borderRadius: 12, paddingHorizontal: 14, minHeight: 48, backgroundColor: '#fff', color: colors.text }, multiline: { minHeight: 110, paddingTop: 13, textAlignVertical: 'top' }, button: { minHeight: 48, paddingHorizontal: 18, borderRadius: 12, backgroundColor: colors.primary, alignItems: 'center', justifyContent: 'center', marginVertical: 6 }, secondary: { backgroundColor: '#edf4ff', borderWidth: 1, borderColor: '#b7d2ff' }, danger: { backgroundColor: colors.danger }, buttonText: { color: '#fff', fontWeight: '800', fontSize: 16 }, secondaryText: { color: colors.primaryDark }, dim: { opacity: .65 }, card: { padding: 16, borderRadius: 16, backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border, marginBottom: 12 } });
